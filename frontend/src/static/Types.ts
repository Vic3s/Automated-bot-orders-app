export type ObjectType = {
        id: string,
        name: string,
        price: number,
        quantity: number,
}

export type AccoutnType = {
        name: string,
        email: string,
        address: string
}